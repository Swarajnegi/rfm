{
  "name": "corpus",
  "version": "1.0.0",
  "description": "A private, offline-first portfolio manager for Indian investors.",
  "main": "index.js",
  "scripts": {
    "build:vendor": "esbuild src/vendor.js --bundle --format=iife --outfile=web/vendor.js --minify",
    "build:boot": "esbuild src/boot.js --bundle --format=iife --outfile=web/boot.js --minify",
    "build:bridge": "esbuild src/capacitor-bridge.js --bundle --outfile=web/plugins.js --minify",
    "build": "npm run build:corpus && npm run build:vendor && npm run build:boot && npm run build:bridge",
    "verify": "node scripts/verify-routes.mjs && node scripts/verify-sheets.mjs && node scripts/verify-motion.mjs && node scripts/verify-sync.mjs && node scripts/verify-typeahead.mjs && node scripts/icon-names.mjs --check && node scripts/audit-polish.mjs && node scripts/verify-offline.mjs --seed && node scripts/verify-offline.mjs",
    "test": "npm run verify",
    "build:corpus": "node -e \"const fs=require('fs');fs.writeFileSync('web/corpus.css','/* driver.js base styles, then the Corpus design system. Concatenated at build so the tutorial overlay works offline. */\\n'+fs.readFileSync('node_modules/driver.js/dist/driver.css','utf8')+'\\n'+fs.readFileSync('src/corpus.css','utf8'))\"",
    "fonts": "node scripts/vendor-fonts.mjs --icons"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Swarajnegi/dadfinanceapp.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Swarajnegi/dadfinanceapp/issues"
  },
  "homepage": "https://github.com/Swarajnegi/dadfinanceapp#readme",
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/app": "^8.1.0",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/local-notifications": "^8.2.0",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2",
    "@capgo/capacitor-native-biometric": "^8.6.2",
    "@formkit/auto-animate": "^0.10.0",
    "alpinejs": "^3.17.3",
    "driver.js": "^1.8.0",
    "gsap": "^3.15.0",
    "lightweight-charts": "^5.2.1",
    "lucide": "^1.46.0",
    "number-flow": "^0.6.2",
    "pdfjs-dist": "^6.3.289"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright": "^1.63.0"
  }
}